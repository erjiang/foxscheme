/*
 * FoxScheme
 *
 * Currently, this file creates the FoxScheme object and nothing else.
 * It is expected that the FoxScheme system is run after this.
 */

var FoxScheme = {};
// handy shorterner $fs
var $fs = FoxScheme;
